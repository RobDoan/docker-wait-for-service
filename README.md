A small bash script to use in [Docker](https://www.docker.com/) for waiting other services up before runing the app.

### Usage:

1. Add script `. <path>/wait_for.sh`
2. Run `wait_for hostname_or_ip port`